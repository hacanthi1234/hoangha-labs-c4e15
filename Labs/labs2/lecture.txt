1. download (ctrl + s)
2. trích vùng quan tâm
3. trích thông tin c?n thi?t


url = "trích link"
output_file_name = "tên.xlsx" (theo filel excel)

1. download website:
1.1. open a connection
conn = urlopen(url)
1.2. read
data = conn.read()

1.3. decode
html_content = data.decode("utf-8") (dây là mã cho unicode)

html_file = open ("dantri.html", "w") #write
html_file.write(html_content)
html_file.close

print(html_content)

t?o file duôi html
html:
gõ html -> enter
<h1> d? t?o tiêu d? </h1>

<p> do?n van </p>

<a href="1 link nào dó"> n?i dunng c?n gõ </a> (anchor t?o du?ng link)
(href là attribute, luôn n?m ? th? m?)

<div></div>: chia vùng

2. extract ROI:
ctr + shift + I: m? console
ctrl F d? m? search code

create a soup:
soup = BeautifulSoup(html_content, "html.parser")
